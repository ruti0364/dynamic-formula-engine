# Copy this file to .env.development for local dev, or .env.production for build
# -----------------------------------------------------------------------

# true  = use static mock data (no backend needed) — for Vercel / demo
# false = connect to live Node.js API on localhost:3001
REACT_APP_USE_MOCK=false
